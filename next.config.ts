import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "cdn.freebiesupply.com", "cdn.iconscout.com", "seeklogo.com", ]
  },  
};

export default nextConfig;
